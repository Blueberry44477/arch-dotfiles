# background {
#     monitor = eDP-1
#     #path = /home/me/someImage.png   # only png supported for now
#     color = rgba(25, 20, 20, 1.0)

#     # all these options are taken from hyprland, see https://wiki.hyprland.org/Configuring/Variables/#blur for explanations
#     blur_passes = 0 # 0 disables blurring
#     blur_size = 7
#     noise = 0.0117
#     contrast = 0.8917
#     brightness = 0.8172
#     vibrancy = 0.1696
#     vibrancy_darkness = 0.0
# }

# input-field {
#     monitor = eDP-1
#     size = 200, 50
#     outline_thickness = 3
#     dots_size = 0.33 # Scale of input-field height, 0.2 - 0.8
#     dots_spacing = 0.15 # Scale of dots' absolute size, 0.0 - 1.0
#     dots_center = false
#     dots_rounding = -1 # -1 default circle, -2 follow input-field rounding
#     outer_color = rgb(151515)
#     inner_color = rgb(200, 200, 200)
#     font_color = rgb(10, 10, 10)
#     fade_on_empty = true
#     fade_timeout = 1000 # Milliseconds before fade_on_empty is triggered.
#     placeholder_text = <i>Input Password...</i> # Text rendered in the input box when it's empty.
#     hide_input = false
#     rounding = -1 # -1 means complete rounding (circle/oval)
#     check_color = rgb(204, 136, 34)
#     fail_color = rgb(204, 34, 34) # if authentication failed, changes outer_color and fail message color
#     fail_text = <i>$FAIL <b>($ATTEMPTS)</b></i> # can be set to empty
#     fail_transition = 300 # transition time in ms between normal outer_color and fail_color
#     capslock_color = -1
#     numlock_color = -1
#     bothlock_color = -1 # when both locks are active. -1 means don't change outer color (same for above)
#     invert_numlock = false # change color if numlock is off
#     swap_font_color = false # see below

#     position = 0, -20
#     halign = center
#     valign = center
# }

# Config 2
# Background
background {
    monitor =
    path = $HOME/.cache/lock_background
    blur_passes = 2
    blur_size = 4
    contrast = 0.8
    brightness = 0.6
    vibrancy = 0.16
    vibrancy_darkness = 0.05
}

# General
general {
}

# Day-Month-Date
label {
    monitor =
    text = cmd[update:10000] echo -e "$(date +"%A, %B %d")"
    color = rgba(227, 227, 227, 0.7)
    font_size = 25
    font_family = Inter Display Bold
    position = 0, 250
    halign = center
    valign = center
}

# Time
label {
    monitor =
    text = cmd[update:10000] echo "<span>$(date +"%I:%M")</span>"
    color = rgba(227, 227, 227, 0.7)
    font_size = 120
    font_family = Inter Display Bold
    position = 0, 150
    halign = center
    valign = center
}

# User-box
shape {
    monitor =
    size = 300, 60
    color = rgba(255, 255, 255, .1)
    rounding = -1
    border_size = 0
    border_color = rgba(255, 255, 255, 0)
    rotate = 0
    xray = false
    position = 0, -50
    halign = center
    valign = center
}

# User
label {
    monitor =
    text =  $USER
    color = rgba(231, 231, 231, 0.8)
    font_size = 18
    font_family = Inter Display Bold
    position = 0, -50
    halign = center
    valign = center
}

# Input Field
input-field {
    monitor =
    size = 300, 60
    outline_thickness = 2
    dots_size = 0.2
    dots_spacing = 0.2
    dots_center = true
    outer_color = rgba(255, 255, 255, 0)
    inner_color = rgba(255, 255, 255, 0.1)
    check_color = rgba(255, 255, 255, 1)
    font_color = rgb(200, 200, 200)
    fade_on_empty = false
    font_family = Inter Display Bold
    placeholder_text = <i><span foreground="##ffffff99">Password</span></i>
    hide_input = false
    position = 0, -130
    halign = center
    valign = center
}

# Reboot
label {
    monitor =
    text = <span> </span>
    color = rgba(255, 255, 255, 0.6)
    font_size = 50
    onclick = reboot
    position = 20, 100
    halign = center
    valign = bottom
}

# Power off
label {
    monitor =
    text = <span> </span>
    color = rgba(255, 255, 255, 0.6)
    font_size = 50
    onclick = shutdown now
    position = 840, 100
    halign = left 
    valign = bottom
}

# Suspend
label {
    monitor =
    text = <span> </span>
    color = rgba(255, 255, 255, 0.6)
    font_size = 50
    onclick = systemctl suspend
    position = -800, 100
    halign = right
    valign = bottom
}

# Battery
label {
    monitor =
    text = cmd[update:10000] sh -c '[ -f /sys/class/power_supply/BAT0/capacity ] || exit; cap=$(cat /sys/class/power_supply/BAT0/capacity); if [ "$cap" -ge 90 ]; then icon=""; elif [ "$cap" -ge 75 ]; then icon=""; elif [ "$cap" -ge 50 ]; then icon=""; elif [ "$cap" -ge 25 ]; then icon=""; else icon=""; fi; status=$(cat /sys/class/power_supply/BAT0/status); [ "$status" = "Charging" ] && icon="󰂄"; echo "$icon $cap%"'
    color = rgba(227, 227, 227, 0.7)
    font_size = 16
    font_family = Inter Display Bold
    position = -20, 20
    halign = right
    valign = bottom
}